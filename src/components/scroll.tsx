import { VelocityScroll } from "@/components/ui/scroll-based-velocity";

export function ScrollBasedVelocity() {
  return <VelocityScroll defaultVelocity={0.5} numRows={3} className="text-6xl">
  Where Ideas Turn Into Brand Reality,
    </VelocityScroll>;
}
